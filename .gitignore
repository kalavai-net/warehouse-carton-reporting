# Secrets — never commit. Holds the Anthropic API key + Gmail app password.
config/secrets.env
.env

# Claude Code LOCAL settings (per-machine; can capture command history incl. keys)
.claude/settings.local.json

# Business DATA files — these stay LOCAL, never in the repo (code only).
*.xlsx
*.xls
*.csv

# Generated data & logs (regenerated by the pipeline on each run)
output/
logs/

# Python / OS noise
__pycache__/
*.pyc
.DS_Store

# Tunnel binary + share password (local-only)
.tools/
